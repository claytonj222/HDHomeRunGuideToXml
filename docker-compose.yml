version: "3.8"
services:
  epg:
    image: hdhomerunguideimage
    container_name: hdhomerunguide_container
    restart: always
    volumes:
      - ./epg:/output
    network_mode: host
    environment:
      HOST: "YOUR IP"
      FILENAME: "/output/tvguide.xml"
      DAYS: "7"
      HOURS: "4"
